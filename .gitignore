# Dependencies
node_modules/
/functions/node_modules/

# Build output
.nuxt/
.output/
dist
dist/
.vite/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase debug + local state
firebase-debug.log
firebase-debug.*.log
*.cache
hosting/.cache
firebase-*.zip
.firebase/
emulator-ui/

# Emulator export data (regenerate locally; not part of repo state)
.emulator-data/

/functions/.env

# Local env (NEVER commit secrets)
.env
.env.*
!env.example
service-account*.json

# Reference data — bulky source artifacts (CSVs, xlsx) and the
# planning/Q&A snapshots that drove the schema. Tracked-in repo docs
# (setup.md, implementation-plan.md) live alongside this ignored dir.
/docs/reference-data/

# OS / editor noise
.DS_Store
.idea/
.vscode/
